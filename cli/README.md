# CLI (Week 5)

`leash mint --budget X --expires Y --allow Z`, `leash attenuate`, `leash spend`,
`leash revoke`, `leash watch`. Thin wrapper around sdk/ts. Not started; see
docs/BUILD_PLAN.md §7 milestones.
