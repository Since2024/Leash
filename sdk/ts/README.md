# TypeScript SDK (Week 5)

`mint()`, `attenuate()`, `spend()`, `revoke()`, `watch()` — thin wrappers around the
Anchor program calls. Not started; see docs/BUILD_PLAN.md §7 milestones. Do not build
ahead of the on-chain program and its invariant tests (§9 non-goals).
