# Integration tests

Anchor/LiteSVM happy-path tests: issue → attenuate → spend → revoke → redeem, exercised
end-to-end against the built programs. Populate starting Week 2 as each instruction in
`leash-program`/`leash-hook` gets real logic (see docs/BUILD_PLAN.md §7 milestones).
